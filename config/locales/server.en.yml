en:
  site_settings:
    marketplace_enabled: "Enable the marketplace plugin"
    marketplace_min_trust_level: "Minimum trust level required to create marketplace listings"
    marketplace_allowed_currencies: "Currency codes (ISO 4217) sellers may use for listings"
    marketplace_offer_expiry_hours: "Number of hours before a Marketplace offer or counteroffer expires"
  marketplace:
    errors:
      category_disabled: "is disabled and cannot be used for listings"
      category_in_use: "This category cannot be deleted because marketplace listings still use it. Disable it instead, or move those listings to another category first."
      stock_exceeded: "must be at least the number of units already reserved or sold"
      inventory_mode_locked: "cannot be changed after the listing has any transaction history"
      too_many_category_fields: "A category cannot have more than 30 listing fields"
      field_key_immutable: "cannot be changed after the field is created"
      field_type_immutable: "cannot be changed after listings have used the field"
      used_select_choice_removed: "cannot remove a choice that existing listings use"
      field_wrong_category: "does not belong to the listing category"
      malformed_fields: "must be an object containing only field keys and scalar values"
      unknown_fields: "contains an unknown, disabled, or cross-category field"
      required_field: "%{label} is required"
      invalid_boolean: "%{label} must be true or false"
      invalid_integer: "%{label} must be a whole number"
      invalid_select: "%{label} is not a valid choice"
      field_too_long: "%{label} cannot be longer than %{count} characters"
      field_html_not_allowed: "%{label} cannot contain HTML"
      html_not_allowed: "cannot contain HTML"
