@AGENTS.md

# Claude Code adapter

`AGENTS.md` is the canonical project instruction/router. Read only the local `AGENTS.md` files for directories involved in the task. Existing Marketplace-specific skills under `.claude/skills/` remain valid and should be preferred when they match. Cross-model procedures live under `.agents/skills/`; read the matching `SKILL.md` only on demand. Keep repository sweeps and large-doc reads bounded.
